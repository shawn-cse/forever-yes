# Audio folder

The default English song is streamed from Wikimedia Commons so the repository does not bundle a commercial copyrighted recording.

To use your own permitted MP3, place it in this folder and update the `<source>` inside the `backgroundMusic` audio element in `index.html`.
